services:
  - type: web
    name: dashboard_git
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: startCommand: gunicorn -b 0.0.0.0:10000 dashboard_git:server
    envVars:
      - key: PORT
        value: 10000
